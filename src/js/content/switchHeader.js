import Tween from 'gsap';

export const switchHeader = (y) => {
	const header = document.querySelector('.header');
	// if (!!header) {
	// 	Tween.to(header, {
	// 		yPercent: y < 50 ? 0 : -100,
	// 		duration: 0.6,
	// 		ease: 'linear',
	// 	});
	// }
}